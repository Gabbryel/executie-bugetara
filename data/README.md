Director de lucru pentru varianta cu un singur depozit.

Pentru datele reale, folosește un depozit **privat** separat și configurează-l
în ecranul „Bază de date (GitHub)". Fiecare lună se salvează ca `AAAA-LL.json`.
